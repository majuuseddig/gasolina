import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    titulo:{
        fontWeight: "bold",
        fontSize: 20,
        color: "#000"
    },
    textoInput:{
        color: "#fff",
        fontWeight: "bold",
    },
    result:{
        color: "#fff",
        fontWeight: "bold",
    }
})